import styled from "styled-components";

export const ErrorMessageWrapper = styled.p`
  text-align: center;
  font-size: 1.2rem;
  font-weight: 500;
  line-height: 1.6rem;
  color: var(--color-red);
`;
